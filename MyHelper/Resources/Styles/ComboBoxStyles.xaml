<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">


    <Style TargetType="ComboBoxItem" x:Key="ComboBoxItemMainStyle">
        <Setter Property="Background" Value="{StaticResource SolidColorBrush_Transparent}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{StaticResource CornerRadius3}"
                            Background="{TemplateBinding Background}" Padding="2,1">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"
                                    Value="{StaticResource SolidColorBrush_GoldenTainoi}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background"
                                    Value="{StaticResource SolidColorBrush_Melon}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBoxItem" x:Key="ComboBoxItem_Add_EditStyle" BasedOn="{StaticResource ComboBoxItemMainStyle}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"
                                    Value="{StaticResource SolidColorBrush_MustartWithPear}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background"
                                    Value="{StaticResource SolidColorBrush_PearWithMustard}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox" x:Key="ComboBoxMainStyle">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="SelectedIndex" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemMainStyle}"/>
        <Setter Property="Background" Value="{StaticResource SolidColorBrush_Mandarin}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SolidColorBrush_RichRedOrange}"/>
        <Setter Property="Tag" Value="{StaticResource ToggleButtonForComboBoxStyle}"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding Key}"
                               FontWeight="{StaticResource FontWeightBold}"
                               Margin="2,0"
                               Style="{StaticResource TextBlockMainStyle}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid x:Name="templateRoot">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"
                                              Width="0"/>
                        </Grid.ColumnDefinitions>
                        <Popup x:Name="PART_Popup" 
                               Grid.ColumnSpan="2" 
                               IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" 
                               Placement="Bottom" 
                               PopupAnimation="Slide" 
                               AllowsTransparency="True">
                            <Border x:Name="dropDownBorder" 
                                    MinWidth="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, 
                                    Path=ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    CornerRadius="{StaticResource CornerRadius5}"
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                <ScrollViewer x:Name="DropDownScrollViewer">
                                    <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" Margin="1"/>
                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                        <ToggleButton Grid.ColumnSpan="2" 
                                      IsChecked="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsDropDownOpen, Mode=TwoWay}"
                                      Style="{TemplateBinding Tag}"/>
                        <ContentPresenter x:Name="contentPresenter" 
                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                          Content="{TemplateBinding SelectionBoxItem}" 
                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                          IsHitTestVisible="false" Margin="{TemplateBinding Padding}" 
                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox" x:Key="ComboBoxForAdd_Edit" BasedOn="{StaticResource ComboBoxMainStyle}">
        <Setter Property="Background" Value="{StaticResource SolidColorBrush_StrongBronzeShade}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SolidColorBrush_PearlBeige}"/>
        <Setter Property="Tag" Value="{StaticResource ToggleButtonForComboBox_Add_EditUserControlStyle}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem_Add_EditStyle}"/>
    </Style>

</ResourceDictionary>