<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:c="clr-namespace:MyHelper.Infrastructure.Commands">
    <Style TargetType="Window" x:Key="MainWindowStyle">
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="IsTabStop"
                Value="False" />
        <Setter Property="ResizeMode"
                Value="CanResizeWithGrip" />
        <Setter Property="UseLayoutRounding"
                Value="True" />
        <Setter Property="TextOptions.TextFormattingMode"
                Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        <Setter Property="WindowStyle"
                Value="SingleBorderWindow" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome 
                              CaptionHeight="30"
                              ResizeBorderThickness="5"
                              GlassFrameThickness="0"
                              UseAeroCaptionButtons="False"
                              NonClientFrameEdges="None" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Border x:Name="PART_Border" Background="#638b27" Padding="1.5,5,5,5">
                        <Grid x:Name="PART_MainGrid">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <UniformGrid Rows="1" Grid.Row="0" HorizontalAlignment="Right">
                                <UniformGrid.Resources>
                                    <Style TargetType="Button" x:Key="CaptionsButtonsStyle">
                                        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
                                        <Setter Property="Padding" Value="5"/>
                                        <Setter Property="Width" Value="32"/>
                                        <Setter Property="CommandParameter" Value="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent" BorderThickness="0" 
                                                            Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}"
                                                            Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                                                        <fa:IconImage x:Name="icon" Icon="{TemplateBinding fa:IconImage.Icon}"
                                                                      IconFont="Solid"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      Foreground="{TemplateBinding Foreground}"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                        <MultiTrigger>
                                                            <MultiTrigger.Conditions>
                                                                <Condition Property="fa:IconImage.Icon" Value="PowerOff"/>
                                                                <Condition Property="IsMouseOver" Value="True"/>
                                                            </MultiTrigger.Conditions>
                                                            <Setter Property="Foreground" Value="LightCoral"/>
                                                        </MultiTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </UniformGrid.Resources>
                                <Button VerticalContentAlignment="Bottom">
                                    <Button.Command>
                                        <c:MinimizeWindowCommand/>
                                    </Button.Command>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource CaptionsButtonsStyle}">
                                            <Setter Property="fa:IconImage.Icon" Value="WindowMinimize"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button x:Name="BtnWindowStateChange">
                                    <Button.Command>
                                        <c:WindowStateChangedCommand/>
                                    </Button.Command>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource CaptionsButtonsStyle}">
                                            <Setter Property="fa:IconImage.Icon" Value="WindowMaximize"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button>
                                    <Button.Command>
                                        <c:CloseWindowCommand/>
                                    </Button.Command>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource CaptionsButtonsStyle}" >
                                            <Setter Property="fa:IconImage.Icon" Value="PowerOff"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </UniformGrid>
                            <AdornerDecorator Grid.Row="1" 
                                              KeyboardNavigation.IsTabStop="False">
                                <ContentPresenter KeyboardNavigation.TabNavigation="Cycle"
                                                                       x:Name="MainContentPresenter"/>
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter Property="fa:IconImage.Icon"
                                    TargetName="BtnWindowStateChange"
                                    Value="WindowRestore"/>
                            <Setter Property="Margin"
                                    TargetName="PART_MainGrid"
                                    Value="7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>