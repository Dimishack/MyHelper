<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp">
    <Style TargetType="Button" x:Key="ForDeleteInListBoxes">
        <Setter Property="Content" Value="X"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="5,0"/>
        <Setter Property="CommandParameter" Value="{Binding}"/>
        <Setter Property="Foreground" Value="Red"/>
        <Style.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="Green"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" x:Key="ForPanels">
        <Setter Property="Margin" Value="3,5"/>
    </Style>
    <Style TargetType="TextBlock" x:Key="ForPanelsInButton">
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

    <Style TargetType="Button" x:Key="ForUGInCreator_Editors">
        <Setter Property="Margin" Value="10,5"/>
        <Setter Property="Padding" Value="35,10"/>
        <Style.Triggers>
            <Trigger Property="IsDefault" Value="True">
                <Setter Property="Content" Value="OK"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Trigger>
            <Trigger Property="IsCancel" Value="True">
                <Setter Property="Content" Value="Cancel"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" x:Key="ButtonMainStyle">
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="#ff6800"
                            Margin="{TemplateBinding Margin}" Padding="{TemplateBinding Padding}"
                            IsEnabled="{TemplateBinding IsEnabled}">
                        <fa:IconImage x:Name="icon" Icon="{TemplateBinding fa:IconImage.Icon}"
                                      Foreground="#ffdb58" Width="16"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ff8243"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#fff4bd"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ffc961"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#ff6800"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ff6800"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#fff4bd"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" x:Key="DeleteElementStyle"
           BasedOn="{StaticResource ButtonMainStyle}">
        <Setter Property="fa:IconImage.Icon" Value="Xmark"/>
    </Style>

    <Style TargetType="Button" x:Key="ButtonSaveStyle">
        <Setter Property="Margin" Value="1"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="#ff6800" 
                            Margin="{TemplateBinding Margin}" Padding="{TemplateBinding Padding}"
                            IsEnabled="{TemplateBinding IsEnabled}">
                        <Grid>
                            <fa:IconImage x:Name="icon" Icon="Save" IconFont="Regular" Width="16"/>
                            <TextBlock x:Name="text" Text="Автосохр." HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Visibility="Collapsed"
                                       FontSize="{TemplateBinding FontSize}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ff8243"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#fff4bd"/>
                        </Trigger>
                        
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Visibility"
                                    TargetName="icon"
                                    Value="Collapsed"/>
                            <Setter Property="Visibility"
                                    TargetName="text"
                                    Value="Visible"/>
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ffb961"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#ff6800"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"
                                    TargetName="border"
                                    Value="#ff6800"/>
                            <Setter Property="Foreground"
                                    TargetName="icon"
                                    Value="#fff4bd"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>