<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="TextBox" x:Key="TextBoxMainStyle">
        <Setter Property="Background" Value="{StaticResource SolidColorBrush_LightGold}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SolidColorBrush_Mandarin}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource SolidColorBrush_BrightOrange}"/>
        <Setter Property="CaretBrush" Value="{StaticResource SolidColorBrush_DarkGreenishYellowGreen}"/>
        <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Foreground" Value="{StaticResource SolidColorBrush_DarkGreenishYellowGreen}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="{TemplateBinding Margin}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="{StaticResource CornerRadius5}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                      VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled"
                                  Margin="3,2"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush"
                        Value="{StaticResource SolidColorBrush_BrightOrange}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush"
                        Value="{StaticResource SolidColorBrush_BrightOrange}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox" x:Key="TextBoxStyle_AdditionalUserControl" BasedOn="{StaticResource TextBoxMainStyle}">
        <Setter Property="Margin" Value="3,3,5,3"/>
        <Setter Property="Effect" Value="{StaticResource WalnutBrownFogEffect}"/>
        <Setter Property="Background" Value="{StaticResource SolidColorBrush_LightSandy}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SolidColorBrush_DarkSandy}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource SolidColorBrush_LightBrownishGray}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush"
                        Value="{StaticResource SolidColorBrush_LightBrownishGray}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush"
                        Value="{StaticResource SolidColorBrush_PearlBeige}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>