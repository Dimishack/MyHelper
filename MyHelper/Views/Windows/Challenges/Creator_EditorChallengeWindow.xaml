<Window x:Class="MyHelper.Views.Windows.Creator_EditorChallengeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHelper.Views.Windows"
        xmlns:commands="clr-namespace:MyHelper.Infrastructure.Commands"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Creator_EditorChallengeWindow" MinHeight="200" Height="200" MaxHeight="300" MinWidth="400" MaxWidth="400">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <Trigger Property="Grid.Column" Value="0">
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="TextBox">
                <Style.Triggers>
                    <Trigger Property="Grid.Column" Value="1">
                        <Setter Property="Margin" Value="0,5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Челлендж: "/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Продолжительность: "/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="Примечание: "/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Challenge}"/>
        <ComboBox Grid.Column="1" Grid.Row="1"
                  ItemsSource="{Binding Durations}"
                  SelectedItem="{Binding Duration}"/>
        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Note}"/>
        <UniformGrid Grid.Column="1" Grid.Row="3" Rows="1"
                     VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="Margin" Value="10,10,0,10"/>
                </Style>
            </UniformGrid.Resources>
            <Button IsDefault="True" Content="OK" FontWeight="Bold"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorChallengeWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="True"/>
                </Button.Command>
            </Button>
            <Button IsCancel="True" Content="Cancel"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorChallengeWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="False"/>
                </Button.Command>
            </Button>
        </UniformGrid>
    </Grid>
</Window>
