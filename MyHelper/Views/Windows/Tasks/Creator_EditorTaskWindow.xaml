<Window x:Class="MyHelper.Views.Windows.Creator_EditorTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHelper.Views.Windows"
        mc:Ignorable="d"
        
        xmlns:commands="clr-namespace:MyHelper.Infrastructure.Commands"
        
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Creator_EditorTaskWindow" Height="230" Width="400">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontSize="16" FontWeight="Bold"
                   Margin="0,5"/>
        <UniformGrid DockPanel.Dock="Bottom" Rows="1" HorizontalAlignment="Right">
            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Padding" Value="35,5"/>
                    <Setter Property="Margin" Value="10,5"/>
                </Style>
            </UniformGrid.Resources>
            <Button FontWeight="Bold" Content="OK"
                    IsDefault="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorTaskWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="True"/>
                </Button.Command>
            </Button>
            <Button Content="Cancel"
                    IsCancel="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorTaskWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="False"/>
                </Button.Command>
            </Button>
        </UniformGrid>
        <Grid Margin="3">
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <Trigger Property="Grid.Column" Value="0">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="5,0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="0,4,0,2"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Задача: "/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Важность/Срочность: "/>
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Срок: "/>
            <TextBlock Grid.Column="0" Grid.Row="3" Text="Группа: "/>
            <TextBlock Grid.Column="0" Grid.Row="4" Text="Примечание: "/>
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Task}"/>
            <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal">
                <CheckBox IsChecked="{Binding Prompt}"/>
                <TextBlock Text="/"/>
                <CheckBox IsChecked="{Binding Important}"/>
            </StackPanel>
            <DatePicker Grid.Column="1" Grid.Row="2" SelectedDate="{Binding Term}" 
                        Language="RU-ru"/>
            <ComboBox Grid.Column="1" Grid.Row="3" ItemsSource="{Binding Groups}" Text="{Binding SelectedGroup}"
                      IsEditable="True" MaxDropDownHeight="120" StaysOpenOnEdit="True" IsTextSearchEnabled="False"/>
            <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding Note}"/>
        </Grid>
    </DockPanel>
</Window>
