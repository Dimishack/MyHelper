<Window x:Class="MyHelper.Views.Windows.SelectStartDateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHelper.Views.Windows"
        mc:Ignorable="d" 
        xmlns:commands="clr-namespace:MyHelper.Infrastructure.Commands"
        ResizeMode="CanMinimize"
        Height="200" Width="400"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <Trigger Property="Grid.Column" Value="0">
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Trigger>
                    <Trigger Property="Grid.Column" Value="1">
                        <Setter Property="Margin" Value="0,5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" Grid.Row="0" Text="Выберите дату"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="18"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Челлендж: "/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="Дата начала: "/>
        <TextBlock Grid.Column="1" Grid.Row="1"
                   Text="{Binding Challenge}"/>
        <DatePicker Grid.Column="1" Grid.Row="2" Language="ru-RU"
                    SelectedDate="{Binding StartDate}"
                    DisplayDateStart="{Binding StartDate, Mode=OneTime}"/>
        <Button Grid.Column="1" Grid.Row="3" IsDefault="True" Content="OK" FontWeight="Bold"
                Margin="0,10" Padding="5"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:SelectStartDateWindow}}">
            <Button.Command>
                <commands:CloseDialogCommand/>
            </Button.Command>
        </Button>
    </Grid>
</Window>
