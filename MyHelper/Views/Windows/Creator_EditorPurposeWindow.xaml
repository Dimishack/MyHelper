<Window x:Class="MyHelper.Views.Windows.Creator_EditorPurposeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHelper.Views.Windows"
        
        xmlns:commands="clr-namespace:MyHelper.Infrastructure.Commands"
        
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        ResizeMode="CanMinimize"
        Title="{Binding Title}" Height="250" Width="375">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontSize="16" FontWeight="Bold"
                   Margin="10"/>
        <UniformGrid DockPanel.Dock="Bottom" Rows="1" HorizontalAlignment="Right">
            <Button Content="Ok" FontWeight="Bold"
                    Padding="35,5" Margin="10,5" IsDefault="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorPurposeWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="True"/>
                </Button.Command>
            </Button>
            <Button Content="Cancel" IsCancel="True"
                    Padding="35,5" Margin="10,5"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=local:Creator_EditorPurposeWindow}}">
                <Button.Command>
                    <commands:CloseDialogCommand DialogResult="False"/>
                </Button.Command>
            </Button>
        </UniformGrid>
        <Grid Margin="5" Height="101">
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <Trigger Property="Grid.Column" Value="0">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="5,0"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="3,4"/>
                    <Setter Property="TextWrapping" Value="Wrap"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Цель"/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Примечание"/>
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Подцели"/>

            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Purpose}"/>
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Note}"/>
            <Button Grid.Column="1" Grid.Row="2" Content="Редактировать"
                    Margin="3,2" IsEnabled="False"/>
        </Grid>
    </DockPanel>
</Window>
