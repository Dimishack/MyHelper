<Window x:Class="MyHelper.Views.Windows.Books.Creator_EditorBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyHelper.Views.Windows.Books"
        xmlns:cmd="clr-namespace:MyHelper.Infrastructure.Commands"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Creator_EditorBookWindow" Height="202" Width="352">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Title}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontWeight="Bold" FontSize="20" Margin="0,10"/>
        <UniformGrid DockPanel.Dock="Bottom" HorizontalAlignment="Right" Rows="1">
            <UniformGrid.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource ForUGInCreator_Editors}"/>
            </UniformGrid.Resources>
            <Button IsDefault="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                <Button.Command>
                    <cmd:CloseDialogCommand DialogResult="True"/>
                </Button.Command>
            </Button>
            <Button IsCancel="True"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                <Button.Command>
                    <cmd:CloseDialogCommand DialogResult="False"/>
                </Button.Command>
            </Button>
        </UniformGrid>
        <Grid>
            <Grid.Resources>
                <Style TargetType="TextBlock" BasedOn="{StaticResource ForCreator_Editors}"/>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <Trigger Property="Grid.Column" Value="1">
                            <Setter Property="Margin" Value="3"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0"
                       Text="Автор:"/>
            <TextBlock Grid.Column="0" Grid.Row="1"
                       Text="Название:"/>
            <TextBlock Grid.Column="0" Grid.Row="2"
                       Text="Кол-во страниц:"/>
            <TextBox Grid.Column="1" Grid.Row="0"
                     Text="{Binding Author}"/>
            <TextBox Grid.Column="1" Grid.Row="1"
                     Text="{Binding NameBook}"/>
            <TextBox Grid.Column="1" Grid.Row="2"
                     Text="{Binding Pages}" KeyDown="TextBox_KeyDown"/>
        </Grid>
    </DockPanel>
</Window>
